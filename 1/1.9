from datetime import datetime

entry = input("Введіть запис: ")

current_time = datetime.now().strftime("%Y-%m-%d %H:%M:%S")

with open("diary.txt", "a", encoding="utf-8") as file:
    file.write(f"{current_time} - {entry}\n")

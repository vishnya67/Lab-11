with open("numbers.txt", "r", encoding="utf-8") as file:
    total = 0

    while True:
        line = file.readline()

        if not line:
            break

        total += float(line)

print(f"Сума чисел: {total:g}")
